// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "sqlite"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id               String    @id @default(cuid())
  name             String    @default("")
  email            String    @unique @default("")
  password         String
  favoritePodcasts Podcast[] @relation("Podcast_favoritedBy")
  createdAt        DateTime? @default(now())
  isAdmin          Boolean   @default(false)
}

model Podcast {
  id          String  @id @default(cuid())
  title       String  @default("")
  audio_uri   String  @default("")
  video_uri   String  @default("")
  artwork     String  @default("")
  lyricist    String  @default("")
  category    String  @default("")
  type        String  @default("audio")
  artist      Artist? @relation("Podcast_artist", fields: [artistId], references: [id])
  artistId    String? @map("artist")
  favoritedBy User[]  @relation("Podcast_favoritedBy")

  @@index([artistId])
}

model Artist {
  id       String    @id @default(cuid())
  name     String    @default("")
  bio      String    @default("")
  photo    String    @default("")
  podcasts Podcast[] @relation("Podcast_artist")
}
